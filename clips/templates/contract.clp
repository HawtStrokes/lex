(deftemplate contract
   (slot contract-id)
   (slot terms)
   (slot parties)
   (slot duration) ;; Start and end dates
   (slot contract-type) ;; Sale, Employment, Lease, etc.
   (slot contract-value)
   (slot dispute-status)) ;; No Dispute, Mediation, Litigation

